//Dashboard.tsx
import React, { useState } from 'react';

const Dashboard: React.FC = () => {
    return (
        <div className="relative ml-2 p-8" >
            <h1>Todo: show notifications, show newest device changes, show newest compliance changes</h1>
        </div>
    );
};

export default Dashboard;
